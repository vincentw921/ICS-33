def all_substrings(strings):
    class SubstringIterator:
        def __init__(self, strings):
            self.strings = strings
            self.len = 
            self.start = 0
        
        def __next__(self):
            